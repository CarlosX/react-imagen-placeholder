export type ImagenPlaceHolderProps = {
  src: string;
  alt: string;
  width: number | string;
  height: number | string;
  imgClassName?: string;
  placeHolder?: boolean;
  placeHolderClassName?: string;
  lazyLoad?: boolean;
};
